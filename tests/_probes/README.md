# One-off probes

These were written to answer a single question during development — "where is the
symbol drawn?", "what does the store look like after ten imports?" — and are kept
because the question may come up again.

They are **not** part of `scripts/test.sh`. Some reference files or builds that no
longer exist. Read them as notes, not as a suite.
